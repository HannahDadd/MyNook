<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".MainActivity">

    <SearchView
            android:id="@+id/edit_search"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/hint_search_string"
            android:textAlignment="center" tools:layout_editor_absoluteX="0dp" android:layout_marginTop="60dp"
            app:layout_constraintTop_toTopOf="parent"/>

    <Button
            android:id="@+id/btn_search"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/button_search"
            tools:layout_editor_absoluteX="0dp" android:layout_marginTop="56dp"
            app:layout_constraintTop_toBottomOf="@+id/txt_search_result"/>

    <TextView
            android:id="@+id/txt_search_result"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textAlignment="center"
            android:textSize="24sp" tools:layout_editor_absoluteX="0dp" android:layout_marginTop="40dp"
            app:layout_constraintTop_toBottomOf="@+id/edit_search"/>
</androidx.constraintlayout.widget.ConstraintLayout>